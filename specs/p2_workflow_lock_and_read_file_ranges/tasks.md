# Tasks

- [x] T1. Encerrar `harness_add` y `harness_update` en `withWorkflowWriteLock()` cubriendo R1-R3.
- [x] T2. Corregir `read_file` para seleccionar rango antes de truncar cubriendo R4-R5.
- [x] T3. Añadir smoke para concurrencia de `harness_add` y lectura tardía en archivo grande cubriendo R6.
- [x] T4. Verificar con `./init.sh` y dejar evidencia SDD.
