# Tasks

- [x] T1. Implementar fail-closed para config inválida en `loadConfig()` cubriendo R1-R2.
- [x] T2. Validar `include` en `search_repo` antes del glob cubriendo R3-R4.
- [x] T3. Alinear el fixture legacy y agregar smoke de regresión para config inválida y `include` inseguro cubriendo R5-R6.
- [x] T4. Verificar typecheck, build, smoke e `init.sh`.
