# Review — token_flow_simulation

- [x] `simulate` usa los builders reales del harness y la misma heurística local `ceil(bytes/4)`.
- [x] El comando soporta `startup`, `activation`, `loop`, `triage` y salida `--json` sin depender de subprocessos de auto-invocación.
- [x] El smoke prueba que `simulate` no contamina `.harness/metrics/session.json`.
- [x] README, help y scaffold relevante quedaron alineados con el comando nuevo.

## Veredicto

APROBADO
