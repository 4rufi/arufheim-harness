# Tasks

- [x] T1. Detectar repo harness activo o explícito durante `setup --global` / `repair --global` cubriendo R1-R2.
- [x] T2. Generar scaffold repo-scoped preferido para `claude-code` y `codex` en el flujo global cubriendo R1-R3.
- [x] T3. Actualizar docs y smoke para el camino híbrido cubriendo R3-R4.
- [x] T4. Verificar con `typecheck`, `build`, `smoke` e `init.sh`, y cerrar la feature.
