# Review — flow_test_and_readme_step

## Resultado

Aprobado.

## Puntos revisados

- [x] El flujo principal del harness ahora incluye explícitamente `tests+README` antes de `done`.
- [x] La regla quedó reflejada en docs vivas, prompts y templates de `init`.
- [x] `setup --update` podrá propagar el cambio porque el marker de agentes subió a `v2`.
- [x] La compatibilidad de lectura no se rompe: `promptBody()` acepta prompts `v1` o `v2`.
- [x] `./init.sh` quedó verde después de los cambios.
